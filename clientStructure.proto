package main;

message Client {
	required int32 id = 1;
	required string name = 2;
	required string email = 3;
	required string country = 4;

	message Mail {
		required string remoteEmail = 1;
		required string body = 2;
	}
	repeated Mail inbox = 5;
}